// AI features have been removed from this application.
// This file is kept as a placeholder to avoid breaking any lingering imports, 
// though it exports a dummy function.

export const generateWelcomeMessage = async (userName: string): Promise<string> => {
  return `Welcome back, ${userName}!`;
};